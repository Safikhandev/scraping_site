/** @type {import('next').NextConfig} */
const nextConfig = {

  images: {
    unoptimized: true,
  },
  output: "export",
};
// module.exports = {
//   output: "export",
// };

module.exports = nextConfig;
